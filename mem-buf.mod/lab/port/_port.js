/*
 * Viewport on the memory core
 */
const _port = {
    Z:      11,
    DNA:   'port/SlideCamera',
    name:  'port',
    scale:  2,
    minPan: 1.3, // relative space we try to guarantee for both target width and height

    autoscale:       true,
    zoomOnPlusMinus: false, 
}
