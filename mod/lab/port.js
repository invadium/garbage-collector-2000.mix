const Z = 0

function init() {
    lab.background = null
}

function draw() {
    // main layer should be transparent, so GL layer will be visible
    ctx.clearRect(0, 0, ctx.width, ctx.height)

    //fill('#404060')
    //rect(0, 0, ctx.width, ctx.height)
}
